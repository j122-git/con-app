# Digital Construction Hub v2

Static mobile-first prototype.

Replace the example system URLs and helpdesk routes in `app.js` with the real project links.

Deploy directly to GitHub Pages or another static host. The prototype intentionally keeps the UX simple: top 3 recent systems, all systems, training and help.
